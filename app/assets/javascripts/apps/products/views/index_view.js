$(document).ready(function() {
  ProductsApp.Views.Index = Marionette.ItemView.extend({
    template: ich.index

    // initialize: function(collection) {
    //   this.products = collection;
    // }

    // Accept a collection in initialize

    // Instantiate a ProductsApp.Views.ProductsCollectionView (collection view)
    // with the collection

    // Instantiate a ProductsApp.Views.ActionsView (item view)
  });
});

