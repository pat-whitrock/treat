!function(){
  <% Dir.glob("#{Rails.root}/app/assets/javascripts/**/templates/**/*.html.haml").each do |template| %>
    <% depend_on(template) %>
    <% p '***********************************' %>
    <% p template %>
    <% p '***********************************' %>
    <% p Tilt.new(template).render.gsub(/\"/, '\\\"').gsub(/\n/,'') %>
    <% p '***********************************' %>
ich.addTemplate(
"<%= File.basename template, '.html.haml' %>",
"<%= Tilt.new(template).render.gsub(/\"/, '\\\"').gsub(/\n/,'') %>"
 );
  <% end %>
}();
